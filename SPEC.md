# S3文化祭 注文・引換券システム 仕様書

## 1. 目的

S3文化祭で、商品の登録、注文受付、引換券印刷、調理側への注文共有、番号呼び出し表示を行う。

この仕様書を実装前の基準にする。実装時はこの `Michael Jackson` フォルダ内だけを編集し、`../pack` は参考として読むだけにする。

## 2. 作る画面

最終的に以下の5画面を作る。

- `setting.html`: 商品設定画面
- `regi.html`: 注文受付画面
- `receipt.html`: 引換券印刷画面
- `kitchen.html`: 調理・提供管理画面
- `monitor.html`: お客さん向け番号表示画面

既存の `order.html` は試作品として扱う。最終実装では `regi.html` に統合する。

## 3. 全体構成

### 3.1 基本方針

複数PCで使うため、`localStorage` だけを本体データの保存場所にはしない。

1台のPCでローカルサーバーを起動し、他のPCは同じLAN内からブラウザで接続する。

例:

```text
レジPC       http://ホストPCのIP:3000/regi.html
キッチンPC   http://ホストPCのIP:3000/kitchen.html
モニターPC   http://ホストPCのIP:3000/monitor.html
設定PC       http://ホストPCのIP:3000/setting.html
```

### 3.2 データ同期

本体データはサーバー側で管理する。

- 商品情報
- 在庫数
- 注文一覧
- 注文状態
- 当日の注文番号カウンター

画面の更新通知は、実装しやすさを優先して `HTTP API + Server-Sent Events` を使う。

- 画面からの操作: `fetch()` でサーバーへ送る
- サーバーから画面への更新通知: `EventSource` で受け取る

WebSocket は必須にしない。双方向通信が必要になった場合のみ検討する。

### 3.3 localStorage の使い道

`localStorage` は補助用途だけに使う。

- 入力途中の商品設定の一時保存
- 画面ごとの表示設定
- サーバーに繋がらない時の警告表示用キャッシュ

注文、在庫、番号の正本はサーバー側に置く。

## 4. データ仕様

### 4.1 商品データ

```js
{
  id: "p001",
  name: "ムーンウォークコーラ",
  price: 200,
  stock: 30,
  enabled: true,
  sortOrder: 1
}
```

ルール:

- 商品は最大9個。
- 商品名は1文字以上。
- 値段は0円以上の整数。
- 在庫は0個以上の整数。
- `enabled: false` の商品は注文画面に表示しない。

### 4.2 注文データ

```js
{
  id: "20260512-001",
  ticketNo: "MJ-001",
  status: "preparing",
  createdAt: "2026-05-12T10:30:00+09:00",
  items: [
    {
      productId: "p001",
      name: "ムーンウォークコーラ",
      unitPrice: 200,
      quantity: 2,
      amount: 400,
      checked: false
    }
  ],
  total: 400
}
```

### 4.3 注文状態

```text
draft      レジ画面で編集中
preparing  注文確定済み、準備中
calling    キッチンで提供ボタン押下済み、呼出中
completed  お客さんに渡し終わった
cancelled  取り消し
```

MVPでは `draft`, `preparing`, `calling`, `completed` を実装する。`cancelled` は余裕があれば実装する。

### 4.4 注文番号

お客さんに見せる番号は `MJ-001` の形式にする。

- 1日ごとに `MJ-001` から始める。
- 画面上と引換券上では `ticketNo` を大きく表示する。
- 内部IDは日付込みで `20260512-001` の形式にする。

## 5. setting 仕様

### 5.1 目的

商品名、値段、在庫数を設定する。

### 5.2 表示

左側:

- 登録済み商品一覧
- 商品名
- 値段
- 在庫数
- 表示中/非表示

右側:

- 商品名入力
- 値段入力
- 在庫数入力
- 登録ボタン
- 変更ボタン
- 削除ボタン
- キャンセルボタン

### 5.3 操作

- 商品は最大9個まで登録できる。
- 9個を超える登録はできない。
- 商品数が多い場合は一覧をスクロールできる。
- 登録・変更・削除はサーバーへ保存する。
- 保存後、`regi.html` の商品ボタンに即反映される。

### 5.4 入力チェック

- 商品名が空なら保存しない。
- 値段が空、または0未満なら保存しない。
- 在庫が空、または0未満なら保存しない。

## 6. regi 仕様

### 6.1 目的

商品ボタンから注文を作り、注文確定時に引換券を印刷する。

### 6.2 画面構成

左側:

- 商品ボタン一覧
- 各ボタンの左上に在庫数
- 中央に商品名
- 商品名の下に値段

右側:

- 注文内容
- 商品名
- 個数
- 金額
- 個数変更ボタン
- 合計金額
- 注文確定ボタン
- クリアボタン

### 6.3 商品追加

- 商品ボタンを押すと注文内容に商品を追加する。
- 同じ商品をもう一度押すと個数を1増やす。
- 商品を追加するたびに、その商品の表示在庫数を1減らす。
- 在庫が0の商品は押せない。

### 6.4 個数変更

- 個数が2個以上の場合、`▲` と `▼` を表示する。
- `▲` を押すと個数を1増やし、在庫を1減らす。
- `▼` を押すと個数を1減らし、在庫を1増やす。
- 個数が1個の場合、`▼` の代わりに `×` を表示する。
- `×` を押すと注文内容から商品を消し、在庫を1戻す。

### 6.5 注文確定

注文確定ボタンを押すと、以下を行う。

1. サーバー側で注文番号を発行する。
2. 注文を `preparing` として保存する。
3. 確定した分だけ在庫を正式に減らす。
4. `kitchen.html` に注文を表示する。
5. `monitor.html` の準備中欄に注文番号を表示する。
6. `receipt.html` を開いて引換券を印刷する。

注文内容が空の場合、注文確定はできない。

### 6.6 印刷

印刷はまずブラウザの印刷機能で行う。

- 紙幅は58mmを基本にする。
- 80mmプリンターの場合はCSSの紙幅を切り替えられるようにする。
- 自動印刷はMVPでは必須にしない。
- 最初は注文確定後に `receipt.html` を開き、スタッフが印刷操作を行う。

## 7. receipt 仕様

### 7.1 目的

お客さんに渡す引換券を印刷する。

### 7.2 表示内容

上から順に表示する。

```text
S3文化祭
注文内容
商品名 x 個数 金額
合計金額
引換番号
日時
```

### 7.3 デザイン

- レシートプリンター向けの縦長白黒デザインにする。
- `引換番号` を一番目立たせる。
- バーコードは表示しない。
- `THANK YOU` は表示しない。
- Michael Jackson らしさは文字や商品名で控えめに表現する。

### 7.4 受け取りデータ

`receipt.html` は注文IDを受け取り、サーバーから注文データを取得して表示する。

例:

```text
receipt.html?id=20260512-001
```

オフライン確認用として、URLパラメータに注文JSONを直接渡す方式も残してよい。

## 8. kitchen 仕様

### 8.1 目的

レジで確定された注文を調理側が確認し、準備完了後に呼び出しへ進める。

### 8.2 表示

注文をカード形式で縦に並べる。

各カードに表示する内容:

- 注文日時
- 引換番号
- 商品名
- 個数
- 金額
- 合計金額

注文数が多い場合はスクロールできる。

### 8.3 チェック

- 商品ごとにチェックボックスを付ける。
- チェックした商品には横線を入れる。
- 全商品にチェックが付くまで提供ボタンは押せない。

### 8.4 提供ボタン

提供ボタンを押すと、注文状態を `calling` に変更する。

変更後:

- `monitor.html` の呼出中欄に注文番号を表示する。
- `kitchen.html` では呼出中として表示する。

### 8.5 受け渡し完了

呼出中の注文には `受け渡し完了` ボタンを表示する。

押すと注文状態を `completed` に変更し、monitorの呼出中欄から消す。

## 9. monitor 仕様

### 9.1 目的

お客さん向けに、準備中と呼出中の注文番号を表示する。

### 9.2 表示

2つの欄を表示する。

- 準備中
- 呼出中

表示内容:

- 注文番号だけを大きく表示する。
- `calling` の番号は `preparing` より目立たせる。
- 商品内容や金額は表示しない。

### 9.3 更新

- `regi.html` で注文確定すると、準備中欄に表示される。
- `kitchen.html` で提供ボタンを押すと、準備中から消えて呼出中へ移る。
- `kitchen.html` で受け渡し完了にすると、呼出中から消える。

## 10. サーバー仕様

### 10.1 役割

ローカルサーバーは以下を行う。

- HTML/CSS/JSの配信
- 商品データの保存
- 注文データの保存
- 注文番号の発行
- 在庫の一元管理
- 画面更新通知

### 10.2 保存形式

外部サービスを使わず、まずはJSONファイル保存にする。

```text
data/products.json
data/orders.json
data/counter.json
```

### 10.3 API

最低限必要なAPI:

```text
GET  /api/products
POST /api/products
PUT  /api/products/:id
DELETE /api/products/:id

GET  /api/orders
GET  /api/orders/:id
POST /api/orders
PUT  /api/orders/:id/status
PUT  /api/orders/:id/items/:productId/check

GET  /api/events
```

`/api/events` は `EventSource` 用の更新通知に使う。

### 10.4 在庫処理

正式な在庫数はサーバーで管理する。

- レジ画面で編集中の在庫減少は画面上の仮表示。
- 注文確定時にサーバーが在庫を確定減算する。
- 複数レジで同時注文が起きた場合、サーバー側の在庫が足りなければ確定を拒否する。

## 11. ファイル構成

最終的な構成案:

```text
Michael Jackson/
  AGENTS.md
  SPEC.md
  server.js
  setting.html
  regi.html
  receipt.html
  kitchen.html
  monitor.html
  css/
    setting.css
    regi.css
    receipt.css
    kitchen.css
    monitor.css
  js/
    api.js
    setting.js
    regi.js
    receipt.js
    kitchen.js
    monitor.js
  data/
    products.json
    orders.json
    counter.json
```

既存の `order.html`, `order.css`, `order.js` は、`regi` 実装時に中身を移すか削除する。

## 12. MVPで必ず作る範囲

最初の完成版では以下を必ず作る。

- 商品設定
- 商品ボタン表示
- 注文追加
- 個数増減
- 在庫表示
- 注文確定
- 引換番号発行
- 引換券表示
- キッチン注文一覧
- 商品チェック
- 提供ボタン
- モニター準備中/呼出中表示
- 受け渡し完了
- LAN内ブラウザ同期

## 13. 後回しでよい範囲

- Firebase連携
- 本格的な自動印刷
- 売上集計画面
- 注文取消の完全対応
- 商品画像
- 複数日分の履歴検索

## 14. 受け入れ条件

実装完了とみなす条件:

1. `setting.html` で商品を登録できる。
2. 登録した商品が `regi.html` にボタンで表示される。
3. 商品ボタンを押すと注文内容に入る。
4. 個数変更に応じて画面上の在庫が増減する。
5. 注文確定で引換番号が発行される。
6. 引換券に日時、注文内容、合計金額、引換番号が表示される。
7. `kitchen.html` に同じ注文が表示される。
8. `monitor.html` の準備中に注文番号が表示される。
9. kitchenで全商品チェック後、提供ボタンを押せる。
10. 提供ボタン後、monitorの呼出中に注文番号が表示される。
11. 受け渡し完了後、monitorから注文番号が消える。
12. `../pack` 配下のファイルが一切変更されていない。

## 15. 確認したいこと

実装前に最終確認したい項目:

1. レシートプリンターの紙幅は58mmでよいか。80mmか。
2. 注文番号は `MJ-001` 形式でよいか。
3. `受け渡し完了` ボタンは kitchen に置く形でよいか。
4. レジPCは1台想定か、複数台の可能性があるか。
5. ホストPCで `node server.js` のようにローカルサーバーを起動する形でよいか。

## 16. 実装順

実装は以下の順番で進める。

### Phase 1: 土台

1. `server.js` を作る。
2. 静的ファイル配信を作る。
3. `data/products.json`, `data/orders.json`, `data/counter.json` を作る。
4. `GET /api/products`, `GET /api/orders`, `GET /api/events` を作る。
5. 画面共通の `js/api.js` を作る。

### Phase 2: 商品設定

1. `setting.html`, `css/setting.css`, `js/setting.js` を作る。
2. 商品登録、変更、削除を実装する。
3. 最大9個制限を入れる。
4. 保存後に他画面へ更新通知を送る。

### Phase 3: レジ

1. 試作品の `order.html` の内容を `regi.html` に移す。
2. 商品ボタンをAPIから読み込む。
3. 注文内容の追加、個数増減、削除を実装する。
4. 仮在庫表示を実装する。
5. 注文確定APIを実装する。

### Phase 4: 引換券

1. `receipt.html` を注文ID受け取り方式に変更する。
2. `receipt.html?id=...` で注文データを取得する。
3. 58mm印刷CSSを整える。
4. レジの注文確定後に引換券を開く。

### Phase 5: キッチン

1. `kitchen.html`, `css/kitchen.css`, `js/kitchen.js` を作る。
2. `preparing` の注文をカード表示する。
3. 商品チェックと横線表示を実装する。
4. 全チェック後だけ提供ボタンを押せるようにする。
5. 提供ボタンで `calling` に変更する。

### Phase 6: モニター

1. `monitor.html`, `css/monitor.css`, `js/monitor.js` を作る。
2. 準備中と呼出中を2列で表示する。
3. 更新通知で自動反映する。
4. 呼出中の番号を大きく目立たせる。

### Phase 7: 仕上げ

1. `kitchen.html` に受け渡し完了を実装する。
2. 在庫不足や通信失敗時のエラー表示を整える。
3. 複数画面を同時に開いて同期確認する。
4. 実プリンターの紙幅に合わせて印刷CSSを調整する。
5. `../pack` が変更されていないことを確認する。
